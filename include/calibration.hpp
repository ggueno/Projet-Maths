#ifndef __CALIBRATION__
#define __CALIBRATION__

//kn::Vector<double> buildVectorA( size_t nb );
//kn::Vector<double> buildVectorB();
//void nonLinearSystemSolver( kn::Vector<double>& a , const kn::Vector<double>& b , const double &X, double (*calculResiduPourKPrecise)(const kn::Vector<double>&, const kn::Vector<double>&), const unsigned int nbMaxIteration );

#endif